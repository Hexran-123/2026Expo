# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

本リポジトリは、国土交通省 国土地理院が主催する「Geoアクティビティコンテスト」（G空間EXPO2027、東京ビッグサイト、2027年3月17日〜19日開催）への応募作品として制作する、地理空間情報を活用したウェブサイト/ウェブサービスのためのものです。募集要項の原本（国土地理院の PDF）は、他者の著作物のためこのリポジトリには置いていません。手元の `C:\LZH\NU\2026Expo_backup\除外ファイル\000269785.pdf` にあります。

### 実行・ビルド

ビルド工程はない（ADR-0002）。素の HTML/CSS/JS をそのまま配信する。
ただし `data/*.json` を `fetch` で読むため、`file://` では動かない。ローカルサーバー経由で開くこと。

```
python -m http.server 8080     # http://localhost:8080/ を開く
```

データ生成スクリプトは `tools/` にある（Node.js、依存パッケージなし）。ふだんは実行不要。
手順と各スクリプトの役割は [README.md](README.md) を参照。

**`css/`・`js/`・`data/` を直したら `demo/*.html`（1 枚デモ 4 枚）も作り直すこと**
（README「サーバーなしで見せる1枚を作る」）。並行セッションでの作り直し忘れが
過去に実際に起きている。`node tools/check-demo-fresh.js` で古いものがないか
確かめられる（pre-commit フックと GitHub Actions からも自動で走る）。

**`board.html` も生成物。** `tools/board-template.html`・`data/choshi/board-spots.json`・
`data/choshi/board-posts.json`・`data/choshi/timeline.json`・`data/board/` を直したら
`node tools/build-board.js` で作り直す
（README「絶景掲示板を作り直す」）。`node tools/check-board-fresh.js` が作り直し忘れを拾う
（pre-commit フックと GitHub Actions からも走る）。

**人が手で書く `data/` のファイルは次の7つ。** それ以外の `data/choshi/` 配下は自動生成なので直接編集しない。

- `data/choshi/spots.json` — 車窓絶景ナビの絶景スポット（S01〜S06）
- `data/choshi/schedule.json` — 銚子電鉄の時刻表。**生成物ではない**（`tools/check-schedule.js` は
  検査するだけで書かない）。ダイヤ改正のたびに手で直す。
- `data/lines.json` — 路線の一覧
- `data/choshi/board-spots.json` — 絶景掲示板の掲示スポット25件（座標・出典・確からしさ・`naviSpotId`・`categories`）。
  同じフォルダにあるが `spots.json` とは別のファイルで、用途も別。
  キャベツ畑など同じ場所を指す項目があるので、片方を直したらもう片方も確かめること（ADR-0005）。
- `data/choshi/board-posts.json` — 掲示している「乗客から届いた写真」の一覧。
  ふだんは審査当番の道具（`tools/publish-posts.js`）が書き足す。
- `data/choshi/timeline.json` — 絶景掲示板の**時間軸**（1923年／1981年度／現在）のデータ。
  **ここに書く数字は、必ず [docs/時間軸_裏取り記録.md](docs/時間軸_裏取り記録.md) に出典があること。**
  出典を書けない数字は書かない（ADR-0006）。
- `data/choshi/sources.json` — 絶景掲示板の**出典タブ**に出す一覧（10件）。
  **載せるのは掲示板が実際に使っているものだけ。** 画面に出ていないものの出典を並べると
  それ自体が嘘になるので、車窓絶景ナビ側だけのもの（時刻表・環境音）は入れない。
  `timeline.json` の `sources` と食い違うと `tools/build-board.js` が止まる。

有楽町線の `data/yurakucho/spots.json` と `data/yurakucho/schedule.json` も、実データに
差し替えたあとは同じく人が保守するファイルである（下の「実データに差し替え済み」を参照）。

`data/` は路線ごとにフォルダを分けてある。`data/choshi/` が作品そのもの。`data/yurakucho/` は**多路線対応の実演用**（`role: "demo"`）。

**作品の対象路線は銚子電鉄だけ（設計書 1 章）。この方針は変わっていない。** 有楽町線を併せて出すのは、展示で「この仕組みは銚子電鉄だけのものではない」ことと、ほぼ全線が地下なので「位置情報が取れないときの振る舞い」（設計書 3.3）を見せるため。GPS の挙動を手元で確かめる用途にも引き続き使う。**応募時（2026年9月9日）も現地展示も、有楽町線を `data/lines.json` に載せたまま出す。**

有楽町線の絶景スポットと時刻表は**実データに差し替え済み**（`dataSource: "real"`）。スポットは実在の 10 件で、うち 8 件は地下にあり車窓からは見えない「[足もとの地理](CONTEXT.md)」として扱う。時刻表は実際の平日ダイヤ。出どころと、入っていないものは `data/yurakucho/spots.json` と `schedule.json` の `_source`・`_note` に書いてある。**`tools/make-test-line.js` を有楽町線に対して実行しないこと**（実データが作り物で上書きされる）。

新しく路線を足すときは、`dataSource` を `"synthetic"` から始め、実データに差し替えてから `"real"` に変える。**作り物のまま審査員や来場者に見せないこと。** この値が `"real"` でない路線は累積人気に数えない。

`role`（展示での位置づけ）と `dataSource`（中身が本物か）は別の軸なので、まとめて判定しないこと。

### 応募作品は 2 画面ある（車窓絶景ナビ／絶景掲示板）

`index.html` の**車窓絶景ナビ**（乗車中・スマートフォン・銚子電鉄の沿線）のほかに、
**絶景掲示板**（旅行前・パソコン専用・銚子エリア全体の掲示スポット25件）がある。設計書 1.1 の表を参照。

- 仕様は [docs/絶景掲示板_設計メモ.md](docs/絶景掲示板_設計メモ.md)。立体地図にした理由は
  [ADR-0005](docs/adr/0005-絶景掲示板だけは斜め視点の立体地図にする.md)。
- **本体は `board.html`（2026-08-19 実装）。生成物なので直接編集しない。**
  直すのは `tools/board-template.html` で、`node tools/build-board.js` で作り直す。
- 車窓絶景ナビとは**地形データ・配色・累積人気の仕組み・`js/photo-post.js` を共有し、
  それ以外のコードは共有しない**（board.html は自前の JS を持つ）。

### 乗客が撮った写真は、掲示板へ出せる（2026-08-19）

**写真の投稿を開けた。** ADR-0004 の追記（2026-08-19）が決定で、入口・約束・当番はそこにある。

- 入口は 2 つだけ。**旅の記録の「絶景掲示板に出す」**（スマホ）と、**掲示板の投稿の窓**（パソコン）。
  **成因カード・撮影の直後には出さないこと。** 入口を増やす提案を自分から入れない。
- **掲示板に出るのは写真だけ。文字は送らない。** 文字投稿（`submit_notes`）は画面から畳んだが
  仕組みは残してある。消さないこと。
- 写真は `js/photo-post.js` を通ってだけ外へ出る。**EXIF を落として長辺1600pxに縮めてから送る。**
  IndexedDB の blob を他の場所から送る処理を書かないこと（設計書 7.1 の境界）。
- **審査を通るまで掲示されない。** `review.html` をパスポートで開いて通し、
  `node tools/publish-posts.js` を走らせ、commit して push するまでが 1 回の当番。
- 2027年4月30日の削除は**サーバーとリポジトリの二か所**で行う。リポジトリ側は履歴ごと
  書き換える（手順は [docs/投稿写真の手放し方.md](docs/投稿写真の手放し方.md)）。

### 掲示板には時間軸がある（2026-08-27実装、2026-09-01に内容を削除）

絶景掲示板は**空間軸／時間軸の二つのモード**を持つ。決定と理由は
[ADR-0006](docs/adr/0006-絶景掲示板に時間軸を持たせる.md)。

**2026-09-01、4つのレイヤー（路線・駅・貨物／海岸線と崖／産業／人の広がり）の中身は
すべて削除した。** タブと1923／1981年度／現在の切り替えは残っているが、選んでも
年の見出し以外は何も出ない（地図は空間軸と同じ地形・路線・駅名のまま）。崖線・
駅の乗車人員・地物（醤油・漁港・畑・崖・採石場）・風車・essay は、コードごと描画を
止めてある。詳しい経緯は ADR-0006 の追記、[絶景掲示板_設計メモ 7章](docs/絶景掲示板_設計メモ.md)
を参照。**中身を書き戻す提案が出たら、まずこの2つを読ませること。**

- **時間軸では写真・投稿・いいね・絞り込みを出さない。** 投稿の口を塞ぐ変更ではない（空間軸に戻せば投稿できる）。この決定は生きている。
- **車窓絶景ナビ（`index.html`）には時間軸を出さない。** これも変わっていない。
- [docs/時間軸_裏取り記録.md](docs/時間軸_裏取り記録.md) は、削除前に調べた数字の出典記録として残してある（画面には出ていない）。

### 環境音は 4 本で確定（凍結解除ずみ）

絶景スポットのテーマに合わせて風・波・水音を流す機能（`js/ambient.js`、設計書 8.3）。
2026-08-13〜14 に実装し、音源への「ただの騒音」「キモイ」という指摘を受けて
差し替え候補を探していたが、**2026-08-16 に「候補はどれも採らず、当初の 4 本のまま出す」で確定**。
同時に凍結（2026-08-14〜16）も解除した。

- 音源は `audio/water-flow.mp3`（産業と水運）・`audio/ocean-waves.mp3`（海と空）・
  `audio/wind-field.mp3`（気候と農業）・`audio/wind-terrain.mp3`（地形）の 4 本。
  テーマ単位で、スポット単位ではない。出典は `audio/README.md`。
- **「地形」をスポット固有 2 本に分ける案は撤回した。** 新しい音源を採らない以上、
  同じ音を 2 か所に置くだけで意味がないため。設計書 8.3 の「スポット固有ではなく
  `theme` 単位（4 種）」は、これで現状と一致している。
- 差し替え候補として置いていたファイル（`audio/wind-chime.mp3` ほか 2 本、
  `audio/_candidates2/` の 6 本）は削除ずみ。**蒸し返さないこと。**

### 投稿は、応募から審査結果までも開けたままにする（2026-08-17 決定）

書類応募（2026年9月9日）から審査結果の通知（2026年10月上旬めやす）までのあいだも、乗客の投稿を
受け付けたままにする。理由と、これが課す運用（確認当番・pg_cron・件数の記録）は ADR-0004 の追記に
書いてある。**投稿の口を塞ぐ変更を自分から入れないこと。**

受け付ける中身は 2026-08-19 に**文字から写真へ入れ替えた**（上の節と ADR-0004 の追記）。
口が開いていること自体は変わっていない。

未決のものは [docs/未解決.md](docs/未解決.md) を参照。

### 銚子市観光協会のフォトダウンロード画像を使うとき（2026-08-18）

規約全文と、今回の用途に照らした逐条チェックは
[docs/銚子市観光協会フォトダウンロード利用規約.md](docs/銚子市観光協会フォトダウンロード利用規約.md) に記録済み。
**画像を追加・表示するコードを書く前に必ず読むこと。** 特に次の2点は禁止事項に直接抵触するので、
知らずに書かないこと。

- **`https://www.choshikanko.com/...` への直接リンク（`<img src>` 等）は禁止。** 画像は必ず
  ダウンロードして `assets/choshi/` 配下に置き、ローカルパスで参照する。
- この写真素材を使って何かを販売する変更（グッズ化・有料印刷物など）はしない。

「画像をフォトコンテストへ応募することは禁止」という条項の解釈については未確認のまま進めている
（[docs/未解決.md](docs/未解決.md) 参照）。

### 主要ドキュメント

作品の設計は以下に確定済みです。作業前に必ず参照してください。

- [docs/設計書.md](docs/設計書.md) — **車窓絶景ナビ**の設計仕様（機能・画面・地図・データ・技術構成）。この画面については**唯一の真実の源**。
- [docs/絶景掲示板_設計メモ.md](docs/絶景掲示板_設計メモ.md) — もう一つの画面**絶景掲示板**の設計仕様。設計書と同じ重さで扱うこと（設計書 1.1 参照）。
- [CONTEXT.md](CONTEXT.md) — 用語集。ここで定義された語を使うこと。
- [docs/将来構想.md](docs/将来構想.md) — **今回は作らないもの**。ここに書かれた機能を実装対象と誤認しないこと。応募書類の「今後の展望」の素材でもある。
- [docs/時間軸_裏取り記録.md](docs/時間軸_裏取り記録.md) — 時間軸で使う数字と年号の**出典**。
  `data/choshi/timeline.json` を直す前に必ず読むこと。
- [docs/adr/](docs/adr/) — 「なぜそう決めたか」の記録。設計を変更する提案をする前に読むこと。

技術スタックは確定済み（素の HTML/CSS/JavaScript、フレームワーク・サーバーなし、地図は SVG の自作イラスト）。詳細と理由は設計書 9 章と ADR を参照。絶景掲示板の地図だけが例外で、CSS の 3D 変形を使う（[ADR-0005](docs/adr/0005-絶景掲示板だけは斜め視点の立体地図にする.md)）。

## コンテストの重要な要件・制約

- **書類応募締切**: 2026年9月9日（水）17時必着
- **書類審査結果通知**: 提出期限から概ね1ヶ月後（2026年10月上旬目安）
- **プレゼン動画・作品予稿（A4 1ページ）提出**: 2027年1月上旬（書類審査通過者のみ）
- **本審査**: 2027年2月中旬（オンラインでのプレゼンテーション・質疑応答）
- **現地展示・プレゼンテーション**: 2027年3月17日〜19日（各日2回、1回2分間のプレゼン）
- 応募書類・プレゼンテーションは日本語必須。応募は1応募者につき原則1点。
- 審査観点: ①独創性・チャレンジ性 ②ニーズ ③地域貢献 ④地理空間情報の活用 ⑤展示イメージ
- アイデア段階のもの、販売促進のみが目的のもの、公序良俗・関係法令（著作権、測量法等）に抵触するものは選考対象外。
- プレゼン動画・作品予稿はコンテストのWebサイトに掲載され、当日は会場でも上映されうるため、公開されて問題ない内容にする必要がある。
- 展示・撮影された写真や動画は国土地理院が広報目的で利用する前提。

これらの制約（特に締切と「地理空間情報の活用」という審査観点）は、機能の優先順位や実装方針を判断する際に踏まえること。

## 企画テーマ（2026年7月時点の検討内容）

Google Driveで共有された議事録4点（`20260714アイデア構図ver2`, `アイデア構図ver1`, `基本情報 (1)`, `作成主題決め (1)`）を要約したもの。今後の議論のベースラインとして扱い、方針が変わったら随時この節を更新すること。

### 制作体制
- 学生2名によるチーム開発。教育機関在籍者中心のため「次世代ルーキー賞」が対象になり得る。
- 担当は大きく二つに分かれる: サイト/アプリの構成・デザイン・プロトタイプ制作・プログラミング・AI活用と、対象路線（銚子電鉄）の絶景スポット調査・地理学的情報収集。
- 2026年8月3日を初期プロトタイプ完成の目標日として設定（本審査等の公式締切2026年9月9日とは別の、チーム内の暫定マイルストーン）。
- チーム自身「プログラミングに自信がない」と認識しており、実装体制（AI活用含む）は今後詰める必要がある。

### コンセプト: ローカル鉄道の車窓絶景ナビ
地方のローカル鉄道に乗る利用者が、車窓から見える「絶景スポット」を逃さず楽しめるようにするWebサイト/アプリ。

- 課題意識: 絶景スポットを調べるには各鉄道会社のHPを個別に見るしかなく面倒。電車は単なる移動手段として捉えられがちで、車窓を意識し続けなくても見逃さない仕組みが欲しい。
- 想定する提供価値:
  - 絶景スポットに近づいたら通知し、進行方向のどちら側の窓から見えるか案内
  - 天候・季節情報と組み合わせて、おすすめの絶景タイミングを提案
  - 各スポットにその土地の文化・風習・地理的成因（地形・産業・気候など）の解説を付与し、単なる観光案内ではなく地理学的な説得力を持たせる
  - ローカル線の経営支援・地域PRという社会的意義も兼ねる
- 審査観点に対する自己評価（チーム内の議論より）:
  - ①独創性・チャレンジ性: 概ね良好だが、チャレンジ要素をもう少し強めたい
  - ②ニーズ: 鉄道大国である日本において一定のニーズあり（対象路線の範囲次第）
  - ③地域貢献: ローカル線支援という形で担保できる
  - ④地理空間情報の活用: **現状はGPS利用のみで弱いという自己認識**。各スポットの地理学的成因解説を厚くすることで補強する方針（下記の銚子電鉄スポット調査はこの補強にあたる）
  - ⑤展示イメージ: 未検討
- 参考にした類似アプリ: スイス・グランド・トレインツアー（パーソナライズ旅程、乗換案内、観光スポット紹介、天気表示、多言語対応、オフライン利用等）

### 対象路線の選定: 銚子電鉄（千葉）を軸に検討
複数のローカル線を比較した結果、銚子電鉄を第一候補、伊豆急行を対抗候補とした。

| 路線 | 評価 | 理由 |
|---|---|---|
| 銚子電鉄（千葉） | 〇 | ぬれ煎餅で有名、"the"ローカル線の代表格、絶景スポットあり |
| 伊豆急行（静岡） | 〇 | 特急列車多数、有名だが距離は遠め |
| 江ノ電（神奈川） | ✕ | 絶景スポット（鎌倉高校前など）が既に有名すぎて自分たちが付加できる価値が薄い |
| 秩父鉄道（埼玉） | ✕ | 本数少なく既にCMで有名、SLはあるが必要性は低いと判断 |
| 真岡鉄道（栃木） | ✕ | 目玉のSLが今期運休中 |
| わたらせ渓谷鉄道・小湊鉄道 | △ | トロッコ等の魅力はあるが観光素材としてはやや弱い |
| 関東鉄道・鹿島臨海鉄道 | ✕ | 決め手に欠ける |

銚子電鉄で洗い出し済みの絶景スポット候補（地理学的解説つき。この解説の厚みが「地理空間情報の活用」の核になる想定）:
- **本銚子駅**: 森のトンネル・桜 → 低地から台地への切通し、日本版「恋のトンネル」（元ネタはウクライナ）
- **笠上黒生駅〜西海鹿島駅間**: トウモロコシ畑/キャベツ畑 → 春キャベツ収穫量日本一の産地、温暖な海沿い台地で稲作（水利用）に不向きな土地条件
- **仲ノ町駅付近**: ヤマサ醤油工場 → 寒暖差の少ない湿潤な海洋性気候が麹菌の働きに適し、利根川東遷後の水運交易で原料調達（千葉・野田市も同様の理由で醤油産地）
- **観音駅〜本銚子駅**: 遠くに海が見える区間
- **君ヶ浜駅・外川駅・犬吠駅付近**: ひまわり畑 → キャベツ畑の休閑期に肥料用として植栽
- **外川駅**: 河津桜・菜の花、2019年に銚子電鉄が植樹した桜並木（ぬれ煎餅・まずい棒等の増収策の一環）

不採用/保留とした地域案:
- 東北（三陸鉄道など）: 震災遺構沿いの路線と組み合わせ、防災・震災教育的価値を持たせる案。ただし防災テーマは他応募との競合が多いと懸念し保留。
- 震災遺構マップ・ハザードマップは過去受賞例/実現済みのテーマとして認識し、差別化が難しいと判断。

### 地図・技術面の検討事項
- 背景地図に地理院地図をそのまま使うと情報過多になるため不採用の方向。オリジナルのビジュアル表現を検討中。
- 地理院地図はピンをドロップした際に位置ずれが起きる問題を把握済み（参考: https://maps.gsi.go.jp/ ）。
- 重ね合わせ表示には地理院地図の「淡色地図」が使いやすいとの案（Geminiによる提案）。
- 「今昔マップ」（新旧地図比較）は当初、著作権面の懸念があり保留としていたが、**2026-08-27 に規約を確認して解除した**。ロゴ入りスクリーンショットのWeb公開は規約が明示的に認めている。逐条チェックは [docs/今昔マップ利用条件.md](docs/今昔マップ利用条件.md)。絶景掲示板の時間軸で図1を使う予定だったが、**2026-09-01 に時間軸の中身を削除したため、いまは使っていない**。
- GISソフト「MANDARA」（https://ktgis.net/mandara/）を参考ツールとして検討。
- 鉄道会社サイトの画像等を情報源にする場合は許諾確認が必要という認識。ユーザー投稿による絶景スポット投稿機能も案として存在。
- 絶景表示は季節・天候によるフィルター機能、通信量を抑えた軽量モード（2D・エフェクト少なめ）とフル機能版の使い分けを検討。
- GPSによる位置特定が必須機能。山間部などGPS圏外での挙動（乗車時刻から到達時刻を逆算して通知する代替案）も検討済み。
- ゲーム的要素（GPSルートで陣地を広げる位置情報ゲーム等）は一度案に出たが方向性としては保留気味。乗車記録の蓄積・育成的要素の可能性を示唆する程度。

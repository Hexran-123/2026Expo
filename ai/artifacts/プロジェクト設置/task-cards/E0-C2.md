# AI-Ready 任務卡：E0-C2 核心データモデル基盤

## Metadata

- 任務：路線・地形・絶景スポットのデータスキーマと生成パイプラインを確立する
- 上層規格：[docs/設計書.md](../../../../docs/設計書.md) 8章
- 上層 Epic：プロジェクト設置（E0）
- 上層 User Story：E0-S2 核心データモデル基盤
- 分軌：不適用
- 前置任務（dependsOn）：なし
- 状態：完了
- 風険等級：低
- Agent owner：（遡及記録。実装は2026年7月の既存コミット群による）
- 人工核准者：luanwei（設計書8章として既に確定済み）

## 目標

`data/route.json`（路線形状・駅位置）、`data/terrain.json`（地形濃淡）、`data/terrain-hillshade.png`（陰影）、`data/spots.json`（絶景スポット）のスキーマと、それぞれを生成する`tools/*.js`パイプラインを確立する。

## 情境包（Context Pack）

- 相関ファイル：`data/route.json`, `data/terrain.json`, `data/terrain-hillshade.png`, `data/spots.json`, `data/source/*.json`, `tools/fetch-osm.js`, `tools/build-route.js`, `tools/fetch-elevation.js`, `tools/build-terrain.js`, `tools/fetch-hillshade.js`, `tools/fetch-features.js`, `tools/build-spot-geometry.js`
- 既存パターン：`data/spots.json`のみ人が手で書き、他は`tools/`で自動生成する分離（README・設計書8章）
- 前提：出典表記（OpenStreetMap／国土地理院／気象庁）はREADME・設計書に記載済み
- 未知事項：なし
- 変更許可範囲：該当なし（完了済みタスクの遡及記録）
- 触れてはいけないもの：`data/spots.json`以外の`data/`配下ファイルを直接編集すること（`tools/`経由で再生成する）

## 需求

- 路線・地形・絶景スポットの3種のデータが、設計書8章のスキーマ通りに存在する。
- `data/spots.json`の`lat`/`lon`/`distanceAlong`/`sideUp`/`sideDown`が実データに基づき算出済み。

## 驗收標準

- `data/spots.json`の6スポット全てに`lat`/`lon`/`distanceAlong`/`sideUp`/`sideDown`/`summary`が入っている（`panels`のみ空でよい。成因カードEpicの担当）。
- `tools/build-spot-geometry.js`の出力（`data/source/spot-geometry.json`）と`data/spots.json`の値が対応している。

## 実装備考

データモデル自体は完了。`data/spots.json`の`panels`が空なのはスキーマの不備ではなくコンテンツ未執筆によるもので、成因カードEpic（E2-C1）の担当。

## 驗證契約

- 手動確認：`data/spots.json`をJSONとしてパースできる（`node -e "JSON.parse(require('fs').readFileSync('data/spots.json'))"`）。
- 手動確認：6スポット全件に必須フィールドが揃っている。

## 完成證據

- 変更したファイル：なし（既存実装の確認のみ）。
- 実行したコマンド：`data/spots.json`のフィールド確認。
- 既知の制限：なし。
- 後続タスク：E2-C1（成因カードの本文執筆、`panels`を埋める）。

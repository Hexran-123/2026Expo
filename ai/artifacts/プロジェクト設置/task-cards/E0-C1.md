# AI-Ready 任務卡：E0-C1 技術骨格初期化

## Metadata

- 任務：プロジェクトの技術骨格（ディレクトリ構成・ビルド不要な配信方式・デプロイ）を確立する
- 上層規格：[docs/設計書.md](../../../../docs/設計書.md) 9章
- 上層 Epic：プロジェクト設置（E0）
- 上層 User Story：E0-S1 技術骨格初期化
- 分軌：不適用（サーバー・ビルドを持たない静的サイトのため前端/後端の区別自体が無い）
- 前置任務（dependsOn）：なし（Epic0はEpic群の根）
- 状態：完了
- 風険等級：低
- Agent owner：（遡及記録。実装は2026年7月の既存コミット群による）
- 人工核准者：luanwei（設計書・ADRとして既に確定済み）

## 目標

`index.html` / `css/style.css` / `js/main.js` / `tools/*.js` / `data/*.json` からなる、ビルド工程を持たない静的サイト構成を確立し、GitHub Pagesで常時公開された状態にする。

## 情境包（Context Pack）

- 相関ファイル：`index.html`, `css/style.css`, `js/main.js`, `tools/*.js`, `README.md`
- 既存パターン：[ADR-0002](../../../../docs/adr/0002-フレームワークとサーバーを持たない.md)（フレームワーク・サーバーを持たない）、[ADR-0001](../../../../docs/adr/0001-地図はタイルを使わず自作イラストとする.md)（地図はSVG自作イラスト）
- 前提：`fetch`で`data/*.json`を読むため`file://`直接開きは不可。`python -m http.server`等のローカルサーバー経由が必須（README記載済み）。
- 未知事項：なし
- 変更許可範囲：該当なし（完了済みタスクの遡及記録）
- 触れてはいけないもの：該当なし

## 需求

- ビルド工程なしで直接配信できる静的ファイル構成。
- `data/*.json`を生成する`tools/*.js`パイプラインが手順どおり動く。
- 公開URLが常時アクセス可能であること。

## 驗收標準

- `python -m http.server`経由で`index.html`が正しく表示される。
- `tools/`配下のスクリプトがREADME記載の順序で実行できる。
- 公開URL（https://hexran-123.github.io/2026Expo/）が200を返す。

## 実装備考

2026-08-01時点で確認：GitHub Pagesは`legacy` build typeで`master`ブランチのルートから既に配信中だった（`.github/workflows`や`netlify.toml`のような設定ファイルが無くても動作する方式のため、リポジトリを見ただけでは気づけなかった）。追加のデプロイ設定作業は不要と判明。

## 驗證契約

- 手動確認：`curl -o /dev/null -w "%{http_code}" https://hexran-123.github.io/2026Expo/` が200を返す。
- 手動確認：`python -m http.server 8080`起動後、`http://localhost:8080/`でサイトが表示される。

## 完成證據

- 変更したファイル：なし（既存実装の確認のみ）。
- 実行したコマンド：`gh api repos/Hexran-123/2026Expo/pages`（source: master/root, status確認）、`curl`でのステータスコード確認。
- テスト出力：`https://hexran-123.github.io/2026Expo/` → 200、`.../tools/kanban/board.html` → 200（2026-08-01確認）。
- 既知の制限：自動テスト・CIパイプラインは未整備（ADR-0002によりビルド工程自体を持たない方針のため、現時点では必須ではない）。
- 後続タスク：なし。

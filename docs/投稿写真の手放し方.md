# 投稿写真の手放し方

乗客から預かった写真は、**2027年4月30日にすべて消す**。これは投稿画面
（`index.html` と `board.html`）に書いてある約束であり、[ADR-0004](adr/0004-投稿と集計のためにサーバーを持ち、ログインは持たない.md)
の決定そのものである。それとは別に、掲示した写真を 1 枚だけ下げることもある。

この文書は二つを扱う。取り違えないこと。

| いつ | 何を | どこを読む |
|---|---|---|
| 随時 | 掲示した写真を 1 枚だけ下げる | [1 枚だけ取り下げる](#1-枚だけ取り下げる随時) |
| 2027年4月30日 | 預かったものを全部消す | [手順](#手順2027年4月30日以降に一度だけ) |

消す先は**二か所**ある。片方だけでは約束を果たしたことにならない。

| どこ | 何が入っているか | 消し方 |
|---|---|---|
| サーバー（Supabase） | まだ掲示していない写真、投稿の記録 | `purge_expired_photos()`（pg_cron が自動で呼ぶ） |
| リポジトリ（GitHub） | 掲示した写真そのもの | **下の手順**。ファイルを消すだけでは足りない |

## なぜファイルを消すだけでは足りないか

掲示した写真は `assets/choshi/board/posts/` に置き、GitHub Pages から配っている。
`git rm` してもコミットの履歴には残るので、**過去のコミットを開けば誰でも取り出せる**。
「4月30日にすべて削除します」と画面に出しておきながら取り出せる状態は、
[ADR-0004 の追記（2026-08-17）](adr/0004-投稿と集計のためにサーバーを持ち、ログインは持たない.md)
が言う「投稿画面に書いた文言と実際の動作が食い違う状態」にあたる。

個人情報保護法も、利用の必要がなくなった個人データを遅滞なく消去するよう求めており
（22条）、本人からの消去の求めに応じることも求めている（35条）。顔が写りうる写真は
その個人データである。

## 1 枚だけ取り下げる（随時）

全部消す日を待たずに、掲示した写真を 1 枚だけ下げることがある。**表とファイルは
必ず同時に消すこと。** 片方だけでは下がらない。

- **`data/choshi/board-posts.json` から行を消すだけ** → 地図からピンは消えるが、
  `assets/choshi/board/posts/<id>` の URL では**まだ誰でも取り出せる**。
  画面から見えないぶん、消したつもりのまま気づけない。
- **ファイルを消すだけ** → 表が生き残り、掲示板に壊れた画像が並ぶ。

どちらも `node tools/build-board.js` が止めて教える（pre-commit フックと
GitHub Actions からも走る）。**止められたら、消し忘れた側を消すこと。**
`--no-verify` で押し通さない。

```sh
# 1. 写真そのもの（board-posts.json の file の値）
git rm assets/choshi/board/posts/<id>.jpg

# 2. data/choshi/board-posts.json から、その id の行を消す（手で消してよい）

# 3. 作り直す。posts の数が減っていることを出力で確かめる
node tools/build-board.js

# 4. commit して push。ここではじめて公開版から消える
git add assets/choshi/board/posts data/choshi/board-posts.json board.html
git commit -m "掲示した写真を取り下げる"
git push
```

**自分たちのテスト投稿なら、ここまでで足りる。** 履歴に残るのは自分たちで
撮ったものなので、下の履歴の書き換えは要らない。

**乗客本人から消してほしいと言われたときは、ここで終わりにしない。**
個人情報保護法 35 条が求めているのは消去であって、非表示ではない。
下の手順 2（`git filter-repo`）を、そのファイルのパスに対して同じように行う。
展示の前であっても、本人の求めに応じるほうが優先する。

なお、サーバー側（Supabase）には掲示済みの写真の中身は残っていない
（`review_mark_published` が掲示のときに捨てている）。残っているのは投稿の記録だけで、
それも消したければ SQL Editor から `delete from photo_submission where id = '<id>'`。

## 手順（2027年4月30日以降に、一度だけ）

現地展示は 2027年3月17〜19日に終わっているので、この時点で履歴を書き換えて困る人はいない。

```sh
# 0. 念のため、いまの状態をどこかへ控えておく（作業を間違えたときの戻り先）
git clone --mirror . ../2026Expo-backup.git

# 1. 掲示している写真とその表を消す
git rm -r assets/choshi/board/posts
node -e "const fs=require('fs');const p='data/choshi/board-posts.json';const d=JSON.parse(fs.readFileSync(p));d.posts=[];fs.writeFileSync(p,JSON.stringify(d,null,2)+'\n')"
node tools/build-board.js
git add data/choshi/board-posts.json board.html
git commit -m "預かった写真を手放す（2027-04-30、ADR-0004）"

# 2. 履歴からも取り除く（git-filter-repo。pip install git-filter-repo で入る）
git filter-repo --invert-paths --path assets/choshi/board/posts --force

# 3. 書き換えた履歴を push する
git push --force origin master

# 4. GitHub 側に残る古い実体を落とす
#    fork や pull request が無ければ、しばらくで回収される。
#    急ぐなら GitHub のサポートに「force push 前の実体を消してほしい」と伝える。
```

## 同じ日にあわせて確かめること

- Supabase の `photo_submission` と `photo_pending` が空になっているか
  （`purge_expired_photos()` が呼ばれているか。pg_cron の登録は
  [003_photos.sql](../supabase/schema/003_photos.sql) の末尾）。
- `note_submission` も同じ日に消える（`purge_expired_notes()`、002_notes.sql）。
- 審査の合言葉を使い終える（`set_review_secret()` で捨て値に入れ替えるか、
  プロジェクトごと畳む）。

## 関連

- [ADR-0004](adr/0004-投稿と集計のためにサーバーを持ち、ログインは持たない.md) — 預かるものと約束
- [絶景掲示板 設計メモ](絶景掲示板_設計メモ.md) — 掲示までの流れ
- `tools/publish-posts.js` — 掲示するときの道具（この文書の逆の作業）
